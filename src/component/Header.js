const Header = () => {
    return (
        <header>
            <h1>Welcome To The Weather App</h1>
            <p>We display the coldest temperature in { 'Tel-aviv, Berlin, and Budapest' }</p>
        </header>
    );
};

export default Header;
